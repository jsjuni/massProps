# Example Small Mass Properties Tree

Example Small Mass Properties Tree

## Usage

``` r
mp_tree_small
```

## Format

An 'igraph' tree whose vertices are named as the values of the `id`
column of a mass properties table and whose directed edges point from
child id to parent id.
